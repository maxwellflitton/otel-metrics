[package]
name = "program"
version = "0.1.0"
edition = "2021"

[dependencies]
t-logging = { path = "../t-logging" }
tokio = { version = "1.43.0", features = ["full"] }
# work on exporting this with the t-logging
tracing = "0.1"
